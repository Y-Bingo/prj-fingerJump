
const { property, ccclass } = cc._decorator;

@ccclass
export default class MenuPanel extends cc.Component {

    protected onLoad() {
        // let mask = new cc.Graphics();
        // mask.fillColor = cc.color( 0, 0, 0, 0.7 );
        // mask.roundRect( 0, 0, 250, 200, 50 );

        // this.node.addChild( mask );

        // graphics.strokeColor = cc.color( 233, 186, 18 );

    }

}